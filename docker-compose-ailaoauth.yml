services:
  hid-oauth-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hid-oauth-gateway
    env_file: .env
    environment:
      HID_OAUTH_MODE: ${HID_OAUTH_MODE}
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      REQUIRE_AUTH: ${REQUIRE_AUTH}
      RUST_LOG: ${RUST_LOG}
      PORT: ${PORT}
      MANAGEMENT_PORT: ${MANAGEMENT_PORT}
    ports:
      - "3000:3000"
      - "8080:8080"
    volumes:
      - ./config.yml:/app/config.yml:ro
    networks:
      - default
    external_links:
      - hid-oauth-postgres:postgres

networks:
  default:
    name: api-getway-ocr-ci_hid-oauth-network
    external: true
