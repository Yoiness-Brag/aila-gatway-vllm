services:
  postgres:
    image: postgres:15-alpine
    container_name: aila-oauth-postgres
    env_file: .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - aila-oauth-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aila_oauth -d aila_oauth"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - aila-oauth-network

  migrations:
    build:
      context: .
      dockerfile: Dockerfile.db.migrations
    container_name: aila-oauth-migrations
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
    networks:
      - aila-oauth-network

  sqlx-prepare:
    image: rust:1.88-trixie
    container_name: aila-oauth-sqlx-prepare
    working_dir: /app
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
    volumes:
      - .:/app
    command: >
      bash -c "cargo install sqlx-cli --version 0.8.6 --no-default-features --features postgres,native-tls --locked &&
               cargo sqlx prepare"
    networks:
      - aila-oauth-network

volumes:
  aila-oauth-postgres-data:

networks:
  aila-oauth-network:
    driver: bridge
